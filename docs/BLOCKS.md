# Блоки конструктора страниц

Справочник по всем типам блоков: что блок делает, чем настраивается, откуда
берёт данные. Источник истины — `app/Core/BlockTypeRegistry.php` (ключи типов и
поля), шаблоны — `templates/blocks/{type}.php`, поля редактора —
`app/Views/admin/pages/block_form.php`, приём формы —
`app/Controllers/Admin/BlockController.php::collectData()`.

Всего типов: **33**.

## Общие настройки (есть у любого блока)

Хранятся в `data` блока со служебным префиксом `_` и обрабатываются в
`BlockRenderer::render()`, а не в шаблонах.

| Ключ | Что делает |
|---|---|
| `_spacing` | Пресет вертикальных отступов: `none` / `small` / `premium` / `max` |
| `_pad_top`, `_pad_bottom` | Независимые отступы сверху и снизу |
| `_bg` | Фон секции: `none` / `light` / `tint` / `navy` |
| `_surface` | Подложка: `flat` или `card` |
| `_fullwidth` | Растянуть секцию на всю ширину экрана |
| `_reveal` | Анимация появления: `fade`, `slide-up/left/right`, `zoom-in`, `stagger` |
| `_visible_from`, `_visible_to` | Расписание показа. Считается на сервере: блок вне окна не попадает в HTML, кэш страницы истекает к ближайшей границе |
| `_visible_device` | Ограничение по устройству — только CSS-классами, потому что кэш страницы общий |
| `custom_css` | Произвольный CSS, автоматически ограничивается селектором `#block-N` |

Заголовок первого уровня на странице один: на `h1` претендуют `hero` и
`person_profile`, первому он и достаётся.

## Текст и разметка

| Тип | Название | Ключевые настройки | Состояние |
|---|---|---|---|
| `text` | Текст | 5 вариантов (обычный, вступление, вводный с принципами, текст+список, текст+цитата), медиа (фото/видео/YouTube), позиционирование кадра | сильный |
| `html` | Произвольный HTML | одно поле, разметка проходит через `HtmlSanitizer` | по назначению |
| `cta` | Призыв к действию | вариант, иконка, фото, кнопка, свои цвета фона/текста/кнопки | сильный |
| `text_image` | Текст + фото | сторона фото, позиция кадра, список пунктов | базовый |
| `icon_text` | Иконка и текст | число колонок, строки внутри карточки | базовый |
| `advantages` | Преимущества | вариант, описание раздела, сетка без дыр (`GridBalance`) | средний |

## Обложка и медиа

| Тип | Название | Ключевые настройки | Состояние |
|---|---|---|---|
| `hero` | Обложка | фон (фото/видео/YouTube/цвет), слайды с автопрокруткой, overlay и его направление, панель под текстом, высота и ширина, позиция текста, две кнопки с иконками, кнопка видео | самый развитый |
| `slider` | Слайдер | заголовок, пропорция кадра (16:9 / 4:3 / 21:9 / как есть), автопрокрутка в секундах, ссылка и подпись у слайда | усилен |
| `media_gallery` | Медиагалерея | источник: вручную / фотоальбомы / видео / оба сразу (вкладки), лимит, ссылка «Все» | средний |
| `partners` | Партнёры | логотипы со ссылками, карусель от 2 элементов | базовый |

## Записи из базы

Эти блоки не хранят контент, а собирают опубликованные записи разделов —
дублировать их вручную не нужно. Язык берётся из текущей локали.

| Тип | Название | Настройки | Источник |
|---|---|---|---|
| `news_latest` | Последние новости | лимит, рубрика, текст и адрес ссылки «Все» | `news` |
| `news_feature` | Новости и аналитика | вариант (карточки / мозаика), лимит, рубрика, ссылка «Все» | `news` |
| `news_docs` | Новости + документы | лента слева (лимит, рубрика), ручной список документов справа | `news` + ручной |
| `projects_list` | Список проектов | лимит, число колонок (2–4), ссылка «Все проекты» | `projects` |
| `team_list` | Список команды | лимит, фильтр по отделу, группировка по отделам | `team_members` |
| `cards_grid` | Карточки | вариант (иконки / фото / категории), колонки, цвета, источник «Проекты» с отметкой «показать на главном» | ручной или `projects` |
| `media_gallery` | Медиагалерея | см. выше | `photo_albums`, `videos` |

Отметка **«Показать на главном»** (`is_featured` у проектов и фотоальбомов)
управляет тем, что попадёт в `cards_grid` и `media_gallery`. Если отмеченных
нет — блок откатывается на последние записи.

## Люди и структура

| Тип | Название | Ключевые настройки | Состояние |
|---|---|---|---|
| `person_profile` | Профиль руководителя | фото, имя, должность, текст, телефон и почта с подписями, кнопка | средний |
| `leader_card` | Карточка руководителя | вкладки «Информация / Биография / Функции» со своими иконками, 5 соцсетей, режим «только иконки» на мобильном | сильный |
| `person_cards` | Карточки персон | список персон, пустая карточка = «Вакантно», сетка без дыр | базовый |
| `bio_education` | Биография + образование | биография, карьера, образование, цитата, встраиваемые виджеты до и после | сильный |
| `org_structure` | Оргструктура | макет (дерево/колонки), число колонок, совет, руководитель, боковые пункты, ветви, сворачивание, сноски | сильный |

## Списки и данные

| Тип | Название | Ключевые настройки | Состояние |
|---|---|---|---|
| `counters` | Счётчики | фон и цвет карточки, размер и позиция иконки, выравнивание, анимация чисел | сильный |
| `timeline` | Хронология | пункты по годам, кнопка, отдельная CTA-карточка с фото | сильный |
| `stages` | Этапы реализации | вариант, описание раздела, ссылка «Все», карусель | средний |
| `faq` | Вопросы и ответы | поиск по вопросам, режим «открыт только один», нативный `details/summary` | сильный |
| `docs_list` | Документы | вариант, колонки, поиск по списку, ссылка «Все», сетка без дыр | сильный |
| `testimonials` | Отзывы | фото, цитата, имя, компания, карусель | базовый |
| `contact_cards` | Контактные карточки | иконка (подбирается по заголовку, если не задана), строки контактов с автоссылками `tel:`/`mailto:`, ссылка | усилен |
| `map_point` | Карта с меткой | статичная картинка, встраиваемая карта, режим загрузки (по клику — чтобы не тянуть чужой скрипт сразу), адрес с кнопкой копирования | сильный |

## Взаимодействие

| Тип | Название | Ключевые настройки |
|---|---|---|
| `form` | Форма | выбор формы из раздела «Формы», макет в одну или две колонки |
| `subscribe` | Подписка на дайджест | заголовок, текст, подпись кнопки; согласие на обработку данных подхватывается из настроек сайта |

## Служебные

| Тип | Название | Ключевые настройки |
|---|---|---|
| `columns` | Колонки | 2–4 колонки, пропорции, зазор; вложенные блоки рендерятся рекурсивно, `columns` внутри `columns` запрещены |
| `anchor_nav` | Якорная навигация | ручной список или автосборка по заголовкам блоков страницы, липкий режим |

## Общие механизмы

- **`SectionHead`** (`app/Core/SectionHead.php`) — единая шапка секции:
  заголовок, вводный текст, ссылка «Все …» и место под инструменты. Ссылка не
  зависит от заголовка.
- **`GridBalance`** (`app/Core/GridBalance.php`) — последний ряд сетки
  растягивается, пустых ячеек справа не остаётся. Работает от 901px, ниже
  раскладку задаёт тема.
- **`ContactLink`** (`app/Core/ContactLink.php`) — телефоны и адреса почты в
  строках контактов становятся ссылками.
- **`BlockVisibility`** — расписание и ограничение по устройству.
- **`UrlGuard`** — все пользовательские ссылки проверяются перед выводом.
- **`Media::picture`** — адаптивные изображения с `srcset` и ленивой загрузкой.

## Что ещё предстоит усилить

Блоки со статусом «базовый» в таблицах выше: `text_image`, `icon_text`,
`testimonials`, `partners`, `person_cards`, `subscribe`. Типовые пробелы —
нет вариантов вёрстки, нет настройки числа колонок, нет единой шапки секции.
